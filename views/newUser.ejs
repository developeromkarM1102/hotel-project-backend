<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Hotel Omkar</title>
    <link rel="stylesheet" href="/hotel.css">
    <style>
        .login-section {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            box-sizing: border-box;
            position: relative;
            font-family: 'Poppins', sans-serif;
        }

        .login-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .login-form-container {
            position: relative;
            z-index: 2;
            width: 100%;
            max-width: 450px;
            text-align: center;
            padding: 40px;
            background: rgba(132, 131, 131, 0.3);
            border: 1px solid rgba(197, 157, 95, 0.3);
            border-radius: 8px;
        }

        .section-title {
            color: #fff;
            font-size: 2.5rem;
            text-transform: capitalize;
            margin-bottom: 30px;
            padding-bottom: .5rem;
            position: relative;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            background: #c59d5f;
            height: .2rem;
            width: 6rem;
        }

        .themed-form .input-group {
            margin-bottom: 20px;
        }

        .themed-form input {
            width: 100%;
            padding: 14px;
            background: transparent;
            border: 1px solid #c59d5f;
            border-radius: 5px;
            color: #fff;
            font-size: 1rem;
            font-family: 'Poppins', sans-serif;
            box-sizing: border-box;
            transition: background-color 0.3s;
        }

        .themed-form input::placeholder {
            color: #ccc;
        }

        .themed-form input:focus {
            outline: none;
            background: rgba(197, 157, 95, 0.1);
        }

        .btn {
            width: 100%;
            margin-top: 20px;
            padding: 14px;
            background: #d7922b;
            border: none;
            border-radius: 5px;
            font-size: larger;
            font-style: oblique;
        }

        .toggle-link {
            margin-top: 25px;
            color: #eee;
        }

        .toggle-link a {
            color: #c59d5f;
            text-decoration: none;
            font-weight: 600;
        }

        .toggle-link a:hover {
            text-decoration: underline;
        }

        /* --- Responsive Design for Mobile Devices --- */
        @media (max-width: 576px) {
            .login-form-container {
                padding: 25px;
                margin: 15px;
                border: none;
            }

            .section-title {
                font-size: 2rem;
            }

            .section-title::after {
                width: 4rem;
            }

            .themed-form input {
                padding: 12px;
            }
        }
    </style>
    </style>
</head>

<body>
    <header>
        <h1>Hotel Restoff</h1>
        <div class="menu-btn" onclick="toggleMenu()"><span id="menuIcon">&#9776;</span></div>
        <nav id="mobileMenu">
            <a href="/">Home</a>
            <a href="/about">About</a>
            <a href="/order">Order Now</a>
            <a href="/contact">Contact</a>
            <a href="/Login" class="login-btn">Login</a>
        </nav>
    </header><br><br><br><br>
    <section class="login-section">
        <div class="login-form-container">

            <h2 class="section-title">Create Account</h2>
            <form id="register-form" class="themed-form" action="/user/newUser" method="POST">
                <div class="input-group">
                    <input type="text" id="register-username" name="username" placeholder="Username" required>
                </div>
                <div class="input-group">
                    <input type="email" id="register-email" name="email" placeholder="Email Address" required>
                </div>
                <div class="input-group">
                    <input type="tel" id="register-phone" name="phone" placeholder="Phone Number" required>
                </div>
                <div class="input-group">
                    <input type="password" id="register-password" name="password" placeholder="Create Password"
                        required>
                </div>
                <button type="submit" class="btn">Register</button>
            </form>
            <p class="toggle-link">
                Already have an account? <a href="/Login">Login here</a>
            </p>

        </div>
    </section>

    <script src="hotel.js"></script>
</body>

</html>